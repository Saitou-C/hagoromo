<!DOCTYPE html>
<html>
  <head>
    <title>Hagoromo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="sticky-top">
      <nav class="navbar" style=" background-color:#e3f2fd;">

        <div class="navbar-brand">
          <h1>Hagoromo</h1>
        </div>

        <div class="container"><!--検索バー-->
          <div class="row form-inline mb-2">
            <div class="mr-3"><%= render 'user/searches/form' %></div>
          </div>

          
            <% if admin_signed_in? %><!--管理者ログインヘッダー-->
              <li class="list-unstyled"><%= link_to "ユーザー一覧", admin_path %></li>
              <li class="list-unstyled"><%= link_to "ログアウト", destroy_admin_session_path, method: :delete %></li>

            <% elsif user_signed_in? %><!--ユーザーログインヘッダー-->
              <li class="list-unstyled"><%= link_to "ホーム", root_path %></li>
              <li class="list-unstyled"><%= link_to "新規記事投稿", new_post_path %></li>
              <li class="list-unstyled"><%= link_to "マイページ", user_path(current_user.id) %></li>
              <li class="list-unstyled"><%= link_to "お気に入り一覧",favorites_user_path(current_user.id) %></li>
              <li class="list-unstyled"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>

            <% else %>
              <li class="list-unstyled"><%= link_to "ホーム", root_path %></li>
              <li class="list-unstyled"><%= link_to "新規登録", new_user_registration_path %></li>
              <li class="list-unstyled"><%= link_to "ログイン", new_user_session_path %></li>
            <% end %>
          

        </div>

      </nav>
    </header>

    <main class="mb-auto">
     <%# deivseのフラッシュメッセージを表示 %>
        <!--<p class="notice"><%#= notice %></p>-->
        <!--<p class="alert"><%#= alert %></p>-->
        <% flash.each do |name, msg| %>
          <% if flash[:alert] %>
          <div class="alert alert-danger"<%= name %>>
            <%= msg %>
          </div>
          <% elsif flash[:success] %>
          <div class="success alert-success "<%= name %>>
           <%= msg %>
          </div>
          <% else notice %>
          <div class="notice alert-success"<%= name %>>
            <%= msg %>
          </div>
          <% end %>
        <% end %>
      <%= yield %>
    </main>

  </body>
</html>
