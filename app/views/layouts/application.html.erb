<!DOCTYPE html>
<html>
  <head>
    <title>Hagoromo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if admin_signed_in? %>
      <header>
        管理者ログインヘッダー
        <li><%= link_to "ユーザー一覧", admin_path %></li>
        <li><%= link_to "ログアウト", destroy_admin_session_path, method: :delete %></li>
      </header>
    <% elsif user_signed_in? %>
      <header>
        ユーザーログインヘッダー
        <li><%= link_to "ホーム", root_path %></li>
        <li><%= link_to "記事一覧", posts_path %></li>
        <li>検索</li>
        <li><%= link_to "新規記事投稿", new_post_path %></li>
        <li><%= link_to "マイページ", user_path(current_user.id) %></li>
        <li><%= link_to "お気に入り一覧",favorites_user_path(current_user.id) %></li>
        <li>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
        </li>
      </header>
    <% else %>
      <header>
        <li>ホーム</li>
        <li><%= link_to "記事一覧", posts_path %></li>
        <li>検索</li>
        <li>新規記事投稿</li>
        <li>マイページ</li>
        <li>
          <%= link_to "新規登録", new_user_registration_path %>
        </li>
        <li>
          <%= link_to "ログイン", new_user_session_path %>
        </li>
      </header>
    <% end %>

   <%# deivseのフラッシュメッセージを表示 %>
      <!--<p class="notice"><%#= notice %></p>-->
      <!--<p class="alert"><%#= alert %></p>-->
      <% flash.each do |name, msg| %>
        <% if flash[:alert] %>
        <div class="alert alert-danger"<%= name %>>
          <%= msg %>
        </div>
        <% elsif flash[:success] %>
        <div class="success alert-success "<%= name %>>
         <%= msg %>
        </div>
        <% else notice %>
        <div class="notice alert-success"<%= name %>>
          <%= msg %>
        </div>
        <% end %>
      <% end %>

    <%= yield %>
  </body>
</html>
